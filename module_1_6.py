# 2. Создайте переменную my_dict и присвойте ей словарь из нескольких пар ключ-значение.
my_dict = {"Алексей": 1990, "Мария": 1985, "Иван": 2000}

# Выведите на экран словарь my_dict.
print(my_dict)

# Выведите на экран одно значение по существующему ключу и одно по отсутствующему.
existing_key = "Алексей"
missing_key = "Ольга"

print(f"{existing_key}: {my_dict.get(existing_key)}")
print(f"{missing_key}: {my_dict.get(missing_key)}")

# Добавьте ещё две произвольные пары в словарь my_dict.
my_dict["Николай"] = 1992
my_dict["Елена"] = 1988

# Удалите одну из пар по существующему ключу и выведите значение из этой пары на экран.
removed_value = my_dict.pop("Мария")
print(removed_value)

# Выведите на экран словарь my_dict.
print(my_dict)

# 3. Создайте переменную my_set и присвойте ей множество, состоящее из разных типов данных с повторяющимися значениями.
my_set = {1, 2, 3, "Привет", 2, 3, "Мир", True}

# Выведите на экран множество my_set.
print(my_set)

# Добавьте 2 произвольных элемента в множество my_set, которых ещё нет.
my_set.add(4.5)
my_set.add("Пока")

# Удалите один любой элемент из множества my_set.
my_set.discard(1)  # Используем discard, чтобы не было ошибки, если элемента нет.

# Выведите на экран измененное множество my_set.
print(my_set)

